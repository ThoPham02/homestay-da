http:
  path: localhost
  port: 8080
database:
  driver: postgres
  host: localhost
  port: 5432
  user: postgres
  password: password
  dbname: homestay_db
  sslmode: disable
  # Legacy source field (optional)
  # source: "host=localhost port=5432 user=postgres password=password dbname=homestay_db sslmode=disable"
